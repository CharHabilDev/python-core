# Exercices

## Exercice 1

Crée un :

```python
defaultdict(int)
```

Puis affiche une clé inexistante.

---

## Exercice 2

Crée un :

```python
defaultdict(list)
```

Puis affiche une clé inexistante.

---

## Exercice 3

Complète :

| Factory | Valeur créée |
| ------- | ------------ |
| `int`   | ?            |
| `list`  | ?            |
| `set`   | ?            |
| `str`   | ?            |

---

## Exercice 4

Associe :

| Factory | Utilisation |
| ------- | ----------- |
| `int`   | ?           |
| `list`  | ?           |
| `set`   | ?           |
| `str`   | ?           |

---

## Exercice 5

Complète :

```text
clé absente
↓
defaultdict(list)
↓
?
```

---

et

```text
clé absente
↓
defaultdict(int)
↓
?
```

---

## Exercice 6

Réponds avec tes propres mots :

1. Qu'est-ce qu'une default factory ?
2. Quel est le rôle de `int` dans `defaultdict(int)` ?
3. Pourquoi `defaultdict(list)` est-il utile ?
4. Cite deux situations où une factory `list` pourrait être utilisée.
5. Dans quel projet personnel pourrais-tu utiliser `defaultdict(list)` ?
