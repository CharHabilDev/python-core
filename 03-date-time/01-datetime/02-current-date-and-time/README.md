# Current Date and Time

The `datetime` module can retrieve the current date and time from the system clock.

The most commonly used methods are `datetime.now()` and `datetime.today()`.

## Key Concepts

- `datetime.now()`
- `datetime.today()`
- Current date and time
- Datetime objects

---

## Status

✅ Completed